<?xml version="1.0"?>
<psalm
    errorLevel="1"
    findUnusedBaselineEntry="true"
    ignoreInternalFunctionArgs="true"
    ignoreInternalFunctionReturnTypes="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
    <projectFiles>
        <directory name="src"/>
        <ignoreFiles>
            <directory name="vendor"/>
        </ignoreFiles>
    </projectFiles>

    <!-- 代码规范规则 -->
    <plugins>
        <!-- 允许对无法推断类型的变量使用 @var 注释 -->
    </plugins>

    <!-- 抑制特定类型检查错误 -->
    <issueHandlers>
        <InternalClass errorLevel="suppress" />
        <InternalMethod errorLevel="suppress" />
        <InternalProperty errorLevel="suppress" />

        <!-- 针对 vendor 目录的报错降级或抑制 -->
        <UnusedClass>
            <errorLevel type="suppress">
                <directory name="vendor" />
            </errorLevel>
        </UnusedClass>

        <!-- 如果你还在报 vendor 里的某些类找不到或属性不存在 -->
        <UndefinedClass>
            <errorLevel type="suppress">
                <directory name="vendor" />
            </errorLevel>
        </UndefinedClass>
    </issueHandlers>
</psalm>
